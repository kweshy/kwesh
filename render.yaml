services:
  - type: web
    name: school-events-app       # the name you want on Render
    env: node                     # this is a Node.js app
    plan: free                    # free plan
    branch: main                  # branch of your GitHub repo
    buildCommand: "npm install"   # how to install dependencies
    startCommand: "node server.js" # how to start the app